{
  "name": "@muralla/common",
  "version": "1.0.2",
  "description": "Shared types, DTOs, and utilities for Muralla Org",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "class-validator": "^0.14.1",
    "class-transformer": "^0.5.1"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "@types/node": "^20.11.0"
  },
  "peerDependencies": {
    "@nestjs/common": "^10.0.0"
  }
}
