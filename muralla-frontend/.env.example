# Frontend environment variables
# Copy to .env and adjust per environment

# Backend API base URL
# In Railway production, set via service variables to:
# VITE_API_BASE_URL=https://{{ Backend.RAILWAY_PUBLIC_DOMAIN }}
VITE_API_BASE_URL="http://localhost:3000"

# Enable demo login/token flows in non-production only
VITE_ENABLE_DEMO="false"

# MercadoPago public key for browser SDK (optional)
# Set in Railway Frontend service variables when enabling payments UI
VITE_MP_PUBLIC_KEY=""
