{
  "$schema": "https://railway.com/railway.schema.json",
  "rootDirectory": "muralla-frontend",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npx --yes serve -s dist -l $PORT",
    "healthcheckPath": "/",
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
